var a = 3, b = 4, name = "prem", c = "123";

sum = a + b;

sub = a - b;

mul = a * b;

div = a / b;

mod = a % b;

power = a ** b;

// ----> string addtion is possible

t = a + name;

p = name + c;

// ----> string multiplication is not possible
res = a * name;

x = name * c;

console.log(sum, sub, mul, div, mod, power, res, t, x, p);



